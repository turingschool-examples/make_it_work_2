<h2>Contestants</h2>

<% @contestants.each do |contestant| %>
  <ul id=<%="contestant-#{contestant.id}"%>>
    <li> <%= contestant.name %> </li>
    <li> Projects: <%= contestant.projects.map {|project| project.name}.join(", ") %>  </li>
  </ul>
<% end %>
