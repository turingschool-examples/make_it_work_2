<h1>Welcome to the index page</h1>

<% @contestants.each do |contestant| %>
  <section id="<%= contestant.id %>">
  <%= contestant.name %>

  <p>Projects: <% contestant.projects.each do |project| %>
                  <%= project.name %>
               <% end %></p>

  </section>
<% end %>
